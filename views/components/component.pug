mixin set_attr(key, value)
    != key + id + ' = "' + value + '";' 
mixin set_global_attr(key, value)
    != key + ' = "' + value + '";'
document
    block content
    - ++id
    script
        | root = document.currentScript.parentNode;
        block script